<p>Calculer les dates clés de la TUP à partir de la date de publication désirée :</p>

<%= simple_form_for :day, url: "/tups1", 
  wrapper: :search_bar_form, 
  html: { class: 'd-flex flex-row' },
  remote: true do |f| %>

    <%= f.input :publication, as: :string, label: false, input_html: {class: "datepicker"} %>
    
    <%= f.button :submit, "Go", class: "btn-primary flex-shrink-1" %>

<% end %>
